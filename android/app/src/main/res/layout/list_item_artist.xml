<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="net.hogelab.musicbrowser.viewmodel.ArtistListItemViewModel" />

        <variable
            name="viewModel"
            type="ArtistListItemViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:onClick="@{viewModel.onClickListItem}">

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:background="#888888"
            android:scaleType="fitCenter"
            android:src="@{viewModel.defaultDrawable}" />

        <TextView
            android:id="@+id/artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="72dp"
            android:text="@{viewModel.artist}" />

        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/artist"
            android:paddingLeft="72dp"
            android:text='@{viewModel.numberOfAlbums + " albums : " + viewModel.numberOfTracks + " tracks"}' />

    </RelativeLayout>

</layout>